# fileflows-plugins

This repository is organized to match the FileFlows community-repository layout:

- `Scripts/` contains FileFlows JavaScript scripts (`Flow/`, `Shared/`, etc.)
- `DockerMods/` contains Docker Mod helper scripts
